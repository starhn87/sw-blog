diff --git a/dist/lib/crypto.js b/dist/lib/crypto.js
index 72147d7fec7e8d07eb65396263eb0769edd4ed73..2f7bea4f3eb0e34ff92bcfa816b6bd5fa22bf67e 100644
--- a/dist/lib/crypto.js
+++ b/dist/lib/crypto.js
@@ -36,5 +36,11 @@ export const crypto = {
      *
      * @type {SubtleCrypto} The subtle cryptographic interface.
      */
-    subtle: isomorphicCrypto.subtle,
+    subtle: {
+        deriveBits: (...args) => isomorphicCrypto.subtle.deriveBits(...args),
+        encrypt: (...args) => isomorphicCrypto.subtle.encrypt(...args),
+        exportKey: (...args) => isomorphicCrypto.subtle.exportKey(...args),
+        generateKey: (...args) => isomorphicCrypto.subtle.generateKey(...args),
+        importKey: (...args) => isomorphicCrypto.subtle.importKey(...args),
+    },
 };
diff --git a/dist/lib/jwt.js b/dist/lib/jwt.js
index a2894d7a7fbc7d500abdac57f6590c5e1374e4ad..89157fc6525df98b95d65f32009cff8507a0e7cd 100644
--- a/dist/lib/jwt.js
+++ b/dist/lib/jwt.js
@@ -28,9 +28,9 @@ export const createJwt = async (jwk, jwtData) => {
     const base64JwtData = base64UrlEncode(JSON.stringify(jwtData));
     const unsignedToken = `${base64JwtInfo}.${base64JwtData}`;
     // Import the private key from the JWK
-    const privateKey = await crypto.subtle.importKey('jwk', jwk, { name: 'ECDSA', namedCurve: 'P-256' }, true, ['sign']);
+    const privateKey = await globalThis.crypto.subtle.importKey('jwk', jwk, { name: 'ECDSA', namedCurve: 'P-256' }, true, ['sign']);
     // Sign the token using the ECDSA algorithm and SHA-256 hash
-    const signature = await crypto.subtle
+    const signature = await globalThis.crypto.subtle
         .sign({ name: 'ECDSA', hash: { name: 'SHA-256' } }, privateKey, new TextEncoder().encode(unsignedToken))
         .then((token) => base64UrlEncode(token));
     // Return the complete JWT
diff --git a/dist/lib/payload.js b/dist/lib/payload.js
index 8b70e021b0aef23e806ad5c6bb558d77ee60ff68..5ebc3c3ae28e80cc70be5e20c9e0fcf3368f9b3d 100644
--- a/dist/lib/payload.js
+++ b/dist/lib/payload.js
@@ -36,7 +36,7 @@ const importClientKeys = async (keys) => {
     if (decodedKey[0] !== 0x04) {
         throw new Error(`Invalid p256dh key: expected uncompressed point format (0x04 prefix) but got 0x${decodedKey[0].toString(16).padStart(2, '0')}`);
     }
-    const p256 = await crypto.subtle.importKey('jwk', {
+    const p256 = await globalThis.crypto.subtle.importKey('jwk', {
         kty: 'EC',
         crv: 'P-256',
         x: base64UrlEncode(decodedKey.slice(1, 33)),
@@ -53,14 +53,14 @@ const importClientKeys = async (keys) => {
  * @returns {Promise<CryptoKey>} A promise that resolves to the derived pseudo-random key.
  */
 const derivePseudoRandomKey = async (auth, sharedSecret) => {
-    const pseudoRandomKeyBytes = await crypto.subtle.deriveBits({
+    const pseudoRandomKeyBytes = await globalThis.crypto.subtle.deriveBits({
         name: 'HKDF',
         hash: 'SHA-256',
         salt: auth,
         // Adding Content-Encoding data info here is required by the Web Push API
         info: new TextEncoder().encode('Content-Encoding: auth\0'),
     }, sharedSecret, 256);
-    return crypto.subtle.importKey('raw', pseudoRandomKeyBytes, 'HKDF', false, [
+    return globalThis.crypto.subtle.importKey('raw', pseudoRandomKeyBytes, 'HKDF', false, [
         'deriveBits',
     ]);
 };
@@ -73,8 +73,8 @@ const derivePseudoRandomKey = async (auth, sharedSecret) => {
  */
 const createContext = async (clientPublicKey, localPublicKey) => {
     const [clientKeyBytes, localKeyBytes] = await Promise.all([
-        crypto.subtle.exportKey('raw', clientPublicKey),
-        crypto.subtle.exportKey('raw', localPublicKey),
+        globalThis.crypto.subtle.exportKey('raw', clientPublicKey),
+        globalThis.crypto.subtle.exportKey('raw', localPublicKey),
     ]);
     return concatTypedArrays([
         new TextEncoder().encode('P-256\0'),
@@ -97,7 +97,7 @@ const deriveNonce = async (pseudoRandomKey, salt, context) => {
         new TextEncoder().encode('Content-Encoding: nonce\0'),
         context,
     ]);
-    return crypto.subtle.deriveBits({ name: 'HKDF', hash: 'SHA-256', salt, info: nonceInfo }, pseudoRandomKey, 12 * 8);
+    return globalThis.crypto.subtle.deriveBits({ name: 'HKDF', hash: 'SHA-256', salt, info: nonceInfo }, pseudoRandomKey, 12 * 8);
 };
 /**
  * Derives a content encryption key using HKDF from the pseudo-random key, salt, and context.
@@ -112,8 +112,8 @@ const deriveContentEncryptionKey = async (pseudoRandomKey, salt, context) => {
         new TextEncoder().encode('Content-Encoding: aesgcm\0'),
         context,
     ]);
-    const bits = await crypto.subtle.deriveBits({ name: 'HKDF', hash: 'SHA-256', salt, info }, pseudoRandomKey, 16 * 8);
-    return crypto.subtle.importKey('raw', bits, 'AES-GCM', false, ['encrypt']);
+    const bits = await globalThis.crypto.subtle.deriveBits({ name: 'HKDF', hash: 'SHA-256', salt, info }, pseudoRandomKey, 16 * 8);
+    return globalThis.crypto.subtle.importKey('raw', bits, 'AES-GCM', false, ['encrypt']);
 };
 /**
  * Maximum payload size after accounting for encryption overhead.
@@ -171,5 +171,5 @@ export const encryptPayload = async (localKeys, salt, payload, target) => {
     const contentEncryptionKey = await deriveContentEncryptionKey(pseudoRandomKey, salt, context);
     const encodedPayload = new TextEncoder().encode(payload);
     const paddedPayload = padPayload(encodedPayload);
-    return crypto.subtle.encrypt({ name: 'AES-GCM', iv: nonce }, contentEncryptionKey, paddedPayload);
+    return globalThis.crypto.subtle.encrypt({ name: 'AES-GCM', iv: nonce }, contentEncryptionKey, paddedPayload);
 };
diff --git a/dist/lib/request.js b/dist/lib/request.js
index 52af1f5f4a06ebbb38a454a40ff9da3c5ec1da8a..b9740ae0526a01f6f3fe4e0e2812932d547bbf86 100644
--- a/dist/lib/request.js
+++ b/dist/lib/request.js
@@ -137,9 +137,9 @@ export async function buildPushHTTPRequest({ privateJWK, message, subscription,
         }),
     };
     // Generate a random salt for encryption
-    const salt = crypto.getRandomValues(new Uint8Array(16));
+    const salt = globalThis.crypto.getRandomValues(new Uint8Array(16));
     // Generate local keys for encryption
-    const localKeys = await crypto.subtle.generateKey({ name: 'ECDH', namedCurve: 'P-256' }, true, ['deriveBits']);
+    const localKeys = await globalThis.crypto.subtle.generateKey({ name: 'ECDH', namedCurve: 'P-256' }, true, ['deriveBits']);
     // Encrypt the payload for the push notification
     const body = await encryptPayload(localKeys, salt, options.payload, subscription);
     // Construct the VAPID headers for the push notification request
diff --git a/dist/lib/shared-secret.js b/dist/lib/shared-secret.js
index c381000019c0685616d3c7f895662b6c16a57771..056ba952ef3dc521d13338d098e4c74bbc0c7648 100644
--- a/dist/lib/shared-secret.js
+++ b/dist/lib/shared-secret.js
@@ -12,6 +12,6 @@ import { crypto } from './crypto.js';
  * @throws {Error} Throws an error if the key derivation fails.
  */
 export const deriveSharedSecret = async (clientPublicKey, localPrivateKey) => {
-    const sharedSecretBytes = await crypto.subtle.deriveBits({ name: 'ECDH', public: clientPublicKey }, localPrivateKey, 256);
-    return crypto.subtle.importKey('raw', sharedSecretBytes, { name: 'HKDF' }, false, ['deriveBits', 'deriveKey']);
+    const sharedSecretBytes = await globalThis.crypto.subtle.deriveBits({ name: 'ECDH', public: clientPublicKey }, localPrivateKey, 256);
+    return globalThis.crypto.subtle.importKey('raw', sharedSecretBytes, { name: 'HKDF' }, false, ['deriveBits', 'deriveKey']);
 };
diff --git a/dist/lib/vapid.js b/dist/lib/vapid.js
index c1291278823d8e61e406cdd007f91f0a605950d1..f692ececec2249272e9924c52a4d65074ec40f76 100644
--- a/dist/lib/vapid.js
+++ b/dist/lib/vapid.js
@@ -18,7 +18,7 @@ import { getPublicKeyFromJwk } from './utils.js';
  */
 export const vapidHeaders = async (options, payloadLength, salt, localPublicKey) => {
     // Export the local public key to a raw format and encode it in Base64 URL format
-    const localPublicKeyBase64 = await crypto.subtle
+    const localPublicKeyBase64 = await globalThis.crypto.subtle
         .exportKey('raw', localPublicKey)
         .then((bytes) => base64UrlEncode(bytes));
     // Get the server public key from the JWK
